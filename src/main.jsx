import React from "react";
import ReactDOM from "react-dom/client";
import SeerDashboard from "./SeerDashboardV2";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SeerDashboard />
  </React.StrictMode>
);
